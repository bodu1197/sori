# SonarCloud Project Configuration
# https://docs.sonarsource.com/sonarcloud/advanced-setup/analysis-parameters/

sonar.projectKey=bodu1197_sori
sonar.organization=bodu1197
sonar.projectName=sori

# Source directories
sonar.sources=frontend/src,backend

# Exclude from ALL analysis (including duplication)
# - Translation files (repetitive structure by design)
# - SQL migrations (similar schema patterns)
# - Mock files
# - Test files
sonar.exclusions=**/locales/**,**/translations.*,**/i18n/**,**/migrations/**/*.sql,**/__mocks__/**,**/__tests__/**,**/*.test.*,**/*.spec.*

# Additional duplication exclusions (CPD = Copy-Paste Detection)
sonar.cpd.exclusions=**/locales/**,**/translations.*,**/i18n/**,**/migrations/**/*.sql

# TypeScript/JavaScript settings
sonar.javascript.lcov.reportPaths=frontend/coverage/lcov.info
sonar.typescript.lcov.reportPaths=frontend/coverage/lcov.info

# Encoding
sonar.sourceEncoding=UTF-8
